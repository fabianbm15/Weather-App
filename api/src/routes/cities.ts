import express from "express";

const router = express.Router();

router.get("/", (_req, res) => {});

router.get("/:id", (req, res) => {});

router.post("/", (req, res) => {});

export default router;
